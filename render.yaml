services:
  - type: web
    name: onionringuniversity-server
    runtime: node
    plan: free
    buildCommand: cd server && npm install --include=dev && npm run build
    startCommand: cd server && node dist/index.js
    healthCheckPath: /health
    envVars:
      - key: NODE_ENV
        value: production
