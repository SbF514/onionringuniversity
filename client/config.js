// Runtime config — update RENDER_URL after first deploy
window.gameConfig = {
  wsHost: 'REPLACE_WITH_RENDER_URL',  // e.g. school-metaverse.onrender.com
  wsPort: '443'                        // Render uses 443 for wss
};
